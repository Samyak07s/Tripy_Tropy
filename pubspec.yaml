name: tripy_tropy
description: "A new Flutter project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.5.3


dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0-dev.3

  firebase_core: ^2.27.0
  firebase_auth: ^4.17.4

  google_sign_in: ^6.1.5
  url_launcher: ^6.2.5

  flutter_dotenv: ^5.1.0  # for .env file
  flutter_markdown: ^0.6.14  # for bold text decoding and stuff

  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^2.0.1
  build_runner: ^2.4.6

  flutter_launcher_icons: ^0.13.1
  
  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/icon/
    - assets/
    - .env
   
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/appicon.png"
